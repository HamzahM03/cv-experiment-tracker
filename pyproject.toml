[project]
name = "cv-experiment-tracker"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2",
    "sqlalchemy",
    "psycopg[binary]",
    "alembic",
    "python-dotenv",
]